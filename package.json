{
  "name": "farscout",
  "version": "0.1.0",
  "description": "ZAO Farcaster research scout - reads Farcaster via HAATZ, reasons with OpenRouter free models + local Ollama, remembers in Bonfire, talks in Discord",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node index.js",
    "bootstrap": "node scripts/bootstrap.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.0",
    "undici": "^6.19.0"
  }
}
